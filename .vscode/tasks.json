{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar y Arrancar de una",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "gcc src/*.c -o MiPrograma.exe -O1 -Wall -I include -L lib -lraylib -lopengl32 -lgdi32 -lwinmm && MiPrograma.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Compila y ejecuta usando CMD para evitar errores de PowerShell"
        }
    ]
}